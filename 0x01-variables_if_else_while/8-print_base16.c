#include<stdio.h>

/**
 * main - print all base16 numbers
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
	{
		putchar(num);
	}

	for (n = 97; n <= 102; n++)
	{
		putchar(num);
	}

	putchar('\n');
	return (0);
}
